# 数据包编写约定

> **目标版本**: 26.2 | **pack_format**: [101, 1]
> **项目命名空间**: `rks_civil`, `rks_lite`, `rks_music`, `rks_extra`, `rks_security`

---

## 核心规则

在编写任何数据包代码（`.mcfunction`, `.json`）之前，**必须**：

1. **不确定语法时先查 RAG**  
   查阅 `memory/rag/` 下对应主题的 wiki 链接，使用 `fetch_webpage` 获取最新文档。

2. **避免已知错误**  
   对照 `memory/syntax-errors.md` 中的易错点清单和错误记录，尤其是当前版本 (26.2) 的常见陷阱。

3. **参考最佳实践**  
   查阅 `memory/techniques.md` 中的技巧总结，优先使用已验证的设计模式。

---

## 知识库索引

| 主题 | 路径 |
|------|------|
| 命令语法 | `memory/rag/commands.md` |
| 组件格式 | `memory/rag/components.md` |
| 战利品表 | `memory/rag/loot-tables.md` |
| 谓词 | `memory/rag/predicates.md` |
| 标签 | `memory/rag/tags.md` |
| 世界生成 | `memory/rag/worldgen.md` |
| 进度 | `memory/rag/advancements.md` |
| Dialog GUI | `memory/rag/dialog.md` |
| 语法错误记录 | `memory/syntax-errors.md` |
| 技巧总结 | `memory/techniques.md` |

---

## 版本敏感项 (26.2)

- `/dialog` 命令为新系统，语法以 Wiki 最新版为准
- JSON 文本颜色使用名称 (`"red"`) 而非格式化代码 (`"§c"`)
- 物品数据使用 **Data Components** 格式，非旧版 NBT
- 函数宏 (`$()`) 只能做值替换，不能做条件判断
- 存储文本优先使用 `storage`，而非 `scoreboard`

---

## 常用资源

- Minecraft Wiki: https://minecraft.wiki/
- 命令完整列表: https://minecraft.wiki/w/Commands
- 数据包格式: https://minecraft.wiki/w/Data_pack
